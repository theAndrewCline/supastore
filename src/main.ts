import App from './App.svelte'

const something_else = {
        my_thing: 'something'

}

something.cool 

const app = new App({
  target: document.body,
  props: {
    name: 'world'
  }
})

export default app
